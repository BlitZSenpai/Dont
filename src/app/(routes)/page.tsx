import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

export default function Home() {
  return (
    <div>
      <Input placeholder="Enter medium blog url" onChange={() => {}} />
      <Button onClick={() => {}}>Search</Button>
    </div>
  );
}
